spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/liz_shop?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8 #?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    url: jdbc:mysql://localhost:3306/liz_shop_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8 #jdbc:h2:tcp://localhost/~/jpashop
    username: liz
    password: Dmlwjd12^^
    driver-class-name: com.mysql.cj.jdbc.Driver #org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create   # 테스트용: 시작할 때 테이블 만들고 끝나면 제거
    properties:
      hibernate:

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
